import React from 'react';

const MyCards = ({ stacks }) => {
    return (
        <div className="myCards-page">
            {stacks.map(stack => (
                
            ))}
        </div>
    )
}